---
title: 后台项目整体说明
description: 从系统规模、分层架构到核心业务模块，全面了解 ATS 系统的全貌
date: 2024-03-15T10:00:00+08:00
lastmod: 2024-03-15T10:00:00+08:00
weight: 1
tags:
  - ATS
  - 轨道交通
  - 系统架构
categories:
  - 轨道交通
  - 技术分享
mermaid: true
photos:
  - https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=metro%20train%20control%20system%20architecture%20diagram%20with%20layered%20modules%2C%20blue%20tech%20style&image_size=landscape_16_9
---
# HFS1 轨道交通控制系统 - 项目整体说明

## 1. 项目概述

### 1.1 项目简介

HFS1 是一套轨道交通列车自动监控系统（ATS）及其相关控制系统的完整软件实现，采用 C++ 开发，主要运行于 Linux 平台（部分模块支持 Windows）。系统采用分布式架构，由多个相对独立又紧密协作的模块组成，覆盖列车监控、调度、控制、通信、协议转换、人机界面等核心功能。

### 1.2 项目规模

| 指标                         | 数值         |
| ---------------------------- | ------------ |
| 总代码文件数（.cpp/.h/.hpp） | 3,288 个     |
| 总代码行数                   | 1,242,780 行 |
| 平均每文件行数               | 377 行       |
| 模块（目录）数量             | 100+ 个      |
| MD 文档数量                  | 75 份        |

### 1.3 系统架构特点

- **分层架构**：基础库 → 通信层 → 协议层 → 业务层 → 应用层
- **模块化设计**：每个目录对应一个功能模块，独立编译，松耦合
- **分布式部署**：多进程协同，基于消息通信
- **高可靠性**：1+N 主备架构、双网冗余、SIL2 安全完整性等级
- **跨平台**：主要支持 Linux（CENTOS），部分模块兼容 Windows

---

## 2. 系统分层架构

```
┌─────────────────────────────────────────────────────────────┐
│                    应用层（Application Layer）               │
│  TDA │ TICM │ TOCM │ TRCM │ TACTCM │ TDSM │ CMSM │ OSM系列 │
├─────────────────────────────────────────────────────────────┤
│                    业务逻辑层（Business Layer）              │
│  LogicTrainTrackPlug │ TrainDiagramPlug │ AtsInterRebuild   │
│  InfoPredict │ ReportTimePlug │ AtoTrainVerifyPlug          │
├─────────────────────────────────────────────────────────────┤
│                    通信层（Communication Layer）             │
│  msg │ brdg/nbrdg/xbrdg │ base │ cluster │ mssync           │
│  UdpProtocolLib_* │ nicmon │ ifstate                        │
├─────────────────────────────────────────────────────────────┤
│                    协议层（Protocol Layer）                  │
│  AtsVobcProtocol │ CtrlCmdProtocol │ IndicationProtocol     │
│  HmiTransProtocol │ DispatchProtocol │ RmProtocol            │
│  SyncProtocol │ AtsZcProtocol │ DiagramProtocol ...         │
├─────────────────────────────────────────────────────────────┤
│                    数据层（Data Layer）                      │
│  RedisMgr │ RedisClient │ db_wrap │ PublicTd │ hiredis      │
├─────────────────────────────────────────────────────────────┤
│                    基础库层（Base Library）                  │
│  tools │ base │ xml │ ftplib │ Framework │ applib           │
│  SIL2_code（安全完整性等级代码）                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 核心业务模块

### 3.1 调度与运行图

| 模块                          | 中文名称         | 主要职责                                   |
| ----------------------------- | ---------------- | ------------------------------------------ |
| **tda**                 | 调度自动化模块   | 调度计划的生成、管理、分发；与外部系统通信 |
| **trcm**                | 列车进路控制模块 | 列车进路计算、选排、控制；路径搜索         |
| **tocm**                | 列车车载控制模块 | 列车车载控制（ATO/ATP）；与VOBC通信        |
| **ticm**                | 列车综合控制模块 | 列车综合控制；多定时任务；门控             |
| **tactcm**              | 列车实际运行控制 | 列车实际运行数据管理                       |
| **TrainDiagramPlug**    | 运行图插件       | 列车运行图绘制和管理                       |
| **LogicTrainTrackPlug** | 逻辑列车轨道插件 | 逻辑列车和轨道关系处理                     |

### 3.2 监控与人机界面

| 模块                       | 中文名称    | 主要职责                       |
| -------------------------- | ----------- | ------------------------------ |
| **HmiTransInfo**     | HMI信息传输 | 人机界面信息传输和绘制信息生成 |
| **HmiTransProtocol** | HMI协议     | HMI传输协议定义和序列化        |
| **osm_plot**         | OSM绘图     | 运行图、轨道图等图形绘制       |
| **osm_ind**          | OSM表示     | 表示信息显示                   |
| **osm_user**         | OSM用户     | 用户操作和权限管理             |
| **osm_dict**         | OSM字典     | 字典和数据定义                 |
| **osm_bmp**          | OSM位图     | 位图资源管理                   |
| **osm_hisinfo**      | OSM历史信息 | 历史信息查询和显示             |
| **osm_dispplan**     | OSM显示计划 | 显示计划管理                   |
| **osm_statisinfo**   | OSM统计信息 | 统计信息收集和显示             |
| **osm_maintain**     | OSM维护     | 系统维护功能                   |
| **OsmMonitor**       | OSM监控     | 系统监控                       |

### 3.3 通信与消息

| 模块                                      | 中文名称 | 主要职责                    |
| ----------------------------------------- | -------- | --------------------------- |
| **msg**                             | 消息模块 | 消息封装、传输、解析        |
| **base**                            | 基础通信 | TCP/IP、Unix Socket通信框架 |
| **brdg**                            | 业务网桥 | 业务消息桥接                |
| **nbrdg**                           | 网络网桥 | 网络消息桥接                |
| **xbrdg**                           | 扩展网桥 | 扩展消息桥接                |
| **cluster** / **cluster_new** | 集群通信 | 集群间通信                  |
| **mssync**                          | 主备同步 | 主备机数据同步              |
| **nicmon**                          | 网卡监控 | 网卡状态监控                |
| **ifstate**                         | 接口状态 | 接口状态管理                |

### 3.4 数据存储与访问

| 模块                  | 中文名称    | 主要职责              |
| --------------------- | ----------- | --------------------- |
| **RedisMgr**    | Redis管理   | Redis连接池和数据管理 |
| **RedisClient** | Redis客户端 | Redis客户端封装       |
| **hiredis**     | hiredis库   | Redis C客户端基础库   |
| **db_wrap**     | 数据库封装  | ODBC数据库操作封装    |
| **PublicTd**    | 公共数据    | 公共数据结构和定义    |

### 3.5 接口与协议

| 模块                          | 中文名称     | 主要职责            |
| ----------------------------- | ------------ | ------------------- |
| **AtsVobcProtocol**     | ATS-VOBC协议 | ATS与车载控制器协议 |
| **CtrlCmdProtocol**     | 控制命令协议 | 列车控制命令协议    |
| **IndicationProtocol**  | 表示协议     | 表示信息协议        |
| **DispatchProtocol**    | 调度协议     | 调度通信协议        |
| **SyncProtocol**        | 同步协议     | 数据同步协议        |
| **AtsZcProtocol**       | ATS-ZC协议   | ATS与区域控制器协议 |
| **AtsToAtsProtocol**    | ATS-ATS协议  | ATS间通信协议       |
| **RmProtocol**          | 远程监控协议 | 远程监控协议        |
| **DiagramProtocol**     | 运行图协议   | 运行图数据协议      |
| **InterfaceProtocol**   | 接口协议     | 通用接口协议        |
| **InfoProcessProtocol** | 信息处理协议 | 信息处理协议        |

### 3.6 接口适配器

| 模块                  | 中文名称 | 主要职责                  |
| --------------------- | -------- | ------------------------- |
| **edci**        | EDC接口  | EDC（设备控制接口）       |
| **idci**        | IDC接口  | IDC（车站标识接口）       |
| **idec**        | IDE接口  | IDE（集中站接口）         |
| **odci**        | ODC接口  | ODC（输出控制接口）       |
| **PSDInter**    | PSD接口  | 屏蔽门接口                |
| **dtiinter**    | DTI接口  | DTI（列车发车指示器）接口 |
| **tcmsinter_A** | TCMS接口 | 列车控制管理系统接口      |
| **imcinter_A**  | IMC接口  | 综合监控中心接口          |

### 3.7 系统支撑

| 模块                | 中文名称 | 主要职责                 |
| ------------------- | -------- | ------------------------ |
| **base**      | 基础库   | 通信、消息、缓冲区、队列 |
| **tools**     | 工具集   | CRC、字符串、文件等工具  |
| **xml**       | XML库    | XML解析（基于tinyxml）   |
| **ftplib**    | FTP库    | FTP客户端                |
| **Framework** | 框架     | 应用框架基础             |
| **applib**    | 应用库   | 应用通用功能             |
| **SIL2_code** | SIL2代码 | 安全完整性等级2相关代码  |
| **CheckLib**  | 检查库   | 数据完整性检查           |

---

## 4. 关键模块详解

### 4.1 HmiTransInfo（HMI信息传输）

HmiTransInfo 模块负责将列车状态、报警、控制等信息转换为 MMI 终端可显示的绘制信息包。

- **HmiTransTask**：完整HMI传输任务，发送所有元素（A1、A2、B、C1-C5等）
- **HmiTransQuickTask**：快速HMI传输任务，只发送牵引制动状态（C1=33）和DTI（N=11）
- **协议类型**：TYPE_INFO_HMI_INDICATION = 0x66
- **功能码**：FUNC_TRAIN_ATP_HMI_ALL = 0x04

详见：[HmiTransInfo模块说明](file:///D:/project/HFS1/Code/Build_All/HmiTransInfo/HmiTransInfo模块说明.md)

### 4.2 trcm（列车进路控制）

负责列车进路的计算、选排、控制和管理，支持路径搜索算法。

- 核心类：`CTrcm`
- 关键功能：进路选排、路径搜索、扣车、跳停、清客
- 通信：与 Idec 系统的双向通信

详见：[trcm模块说明](file:///D:/project/HFS1/Code/Build_All/trcm/trcm模块说明.md)

### 4.3 tocm（列车车载控制）

负责列车车载控制（ATO/ATP），与VOBC通信。

- 核心类：`CTocm`
- 关键功能：车载控制、移动授权、紧急制动

详见：[tocm模块说明](file:///D:/project/HFS1/Code/Build_All/tocm/tocm模块说明.md)

### 4.4 ticm（列车综合控制）

负责列车综合控制，包括多种定时任务。

- 核心类：`CTicm`
- 关键定时器：基础定时器（1s）、AOM定时器（500ms）、滑行告警（3s）、门控（5s/10s）

详见：[ticm模块说明](file:///D:/project/HFS1/Code/Build_All/ticm/ticm模块说明.md)

### 4.5 tda（调度自动化）

调度系统核心模块，处理调度计划和命令。

- 核心类：`CTda`
- 关键功能：调度计划管理、自动检查、计划分发

详见：[tda模块说明](file:///D:/project/HFS1/Code/Build_All/tda/tda模块说明.md)

---

## 5. 核心业务信息流

### 5.1 跳停业务

```
调度员操作 → OSM → tda → trcm（RouteCancelTask）
                              ↓
                          进路取消命令
                              ↓
                          车载（VOBC）→ tocm → ticm
                              ↓
                          跳停状态 → HmiTransInfo → MMI显示
```

### 5.2 清客业务

```
调度员操作 → tda → trcm → 紧急制动原因第22位（制动重故障）
                              ↓
                          HmiTransInfo（C4区判断）→ MMI显示
```

### 5.3 扣车业务

```
调度员操作 → tda → trcm（HoldTrainTask）→ 车载
                              ↓
                          HmiTransQuickTask → MMI显示
```

### 5.4 进路选排

```
自动触发/人工操作 → trcm（AutoTriggerTask/RouteSelectTask）
                              ↓
                       PathSearch路径搜索
                              ↓
                       RoutesMgr进路管理
                              ↓
                       联锁（CBI）→ 进路执行
```

### 5.5 移动授权

```
ticm → AtsZcProtocol → ZC（区域控制器）
                              ↓
                       移动授权计算
                              ↓
                       AtsVobcProtocol → VOBC → 列车控制
```

---

## 6. 通信机制

### 6.1 进程间通信（IPC）

- **消息总线（msg）**：基于 `CMsgPack`、`CMsgHead` 的消息封装
- **业务网桥（brdg）**：进程间业务消息桥接
- **网络网桥（nbrdg）**：跨节点网络消息桥接
- **集群通信（cluster）**：集群内多机通信

### 6.2 数据同步

- **Redis**：作为共享数据存储，支持多进程共享状态
- **mssync**：主备机数据实时同步
- **SyncProtocol**：同步协议封装

### 6.3 外部接口

- **TCP/UDP**：与外部系统通信
- **ODBC（db_wrap）**：数据库访问
- **FTP（ftplib）**：文件传输

---

## 7. 关键技术点

### 7.1 协议设计

- **协议头**：9字节，包含类型码、功能码、版本、包数、当前包、长度
- **序列化/反序列化**：每个协议类实现 `ToStreamBuf` 和 `FromStreamBuf`
- **字节序**：小端序（Little-Endian）

### 7.2 安全完整性（SIL2）

- **SIL2_code 目录**：包含符合 SIL2 安全完整性等级的代码
- **覆盖范围**：AtsVobcProtocol、SpeedLimitProtocol、traininter、SpeedLimitProtocol
- **关键特性**：数据校验、冗余处理、故障检测

### 7.3 高可用设计

- **1+N 主备架构**：支持多机热备
- **双网冗余**：通信链路冗余
- **故障检测**：通过 `nicmon`、`ifstate` 监控通信状态
- **状态恢复**：通过 `mssync` 实现主备数据同步

---

## 8. 编译与构建

### 8.1 构建系统

- **Linux**：基于 Makefile
- **Windows**：支持 Visual Studio 工程
- **构建脚本**：`script/build.sh`

### 8.2 主要构建产物

- **可执行文件**：tda、ticm、tocm、trcm、tactcm、tdsm、cmsm 等
- **动态库**：libmsg、libbase、libRedisMgr 等
- **配置文件**：各模块的 `.xml` 配置文件

---

## 9. 文档导航

### 9.1 项目级文档

| 文档名称               | 路径                          | 说明         |
| ---------------------- | ----------------------------- | ------------ |
| 整体说明               | `整体说明.md`               | 本文档       |
| 系统模块关系与架构分析 | `系统模块关系与架构分析.md` | 详细架构分析 |
| 业务信息流分析         | `业务信息流分析.md`         | 业务流程分析 |
| 代码统计报告           | `代码统计报告.md`           | 项目代码统计 |
| 需求和变化文档         | `需求和变化文档.md`         | 需求变更记录 |

> **注意**：上述文档中部分文件存在历史编码问题（中文已损坏），完整内容请参考 `MD文档汇总` 目录下的汇总版本。

### 9.2 模块级文档

每个核心模块目录下都有 `<模块名>模块说明.md` 文件，详细说明该模块的功能、文件、接口和依赖关系。

完整列表参见：[MD文档汇总索引](file:///D:/project/HFS1/Code/Build_All/MD文档汇总/00_索引_整体说明.md)

---

## 10. 文档汇总目录说明

为方便查阅，已将项目中所有 MD 文档汇总至：`D:\project\HFS1\Code\Build_All\MD文档汇总`

### 10.1 汇总目录结构

- 文件命名规则：`<原目录路径>.md`（路径分隔符替换为下划线）
- 编码：UTF-8 with BOM（统一编码，避免乱码）
- 索引文件：`00_索引_整体说明.md`

### 10.2 编码处理说明

汇总过程中对原始文档的编码进行了统一处理：

| 处理类型   | 说明                     |
| ---------- | ------------------------ |
| UTF-8 文档 | 保持原样，添加 BOM 标记  |
| GBK 文档   | 自动转换为 UTF-8         |
| 已损坏文档 | 标记为乱码（中文已丢失） |

### 10.3 已损坏的文档（5份）

以下文档因历史保存时编码处理错误，中文内容已全部变为 `?` 字符，无法恢复：

1. `业务信息流分析.md`
2. `代码统计报告.md`
3. `整体说明.md`（本文件为新重写版本）
4. `系统模块关系与架构分析.md`
5. `需求和变化文档.md`

> **建议**：上述文档如需完整内容，建议根据代码和实际业务重新编写。

---

## 11. 维护与更新

- **文档维护**：每次代码修改后，应同步更新对应的模块说明文档
- **变更记录**：在 `需求和变化文档.md` 中记录每次重要变更
- **版本管理**：建议将 MD 文档纳入 Git 版本管理，避免再次损坏

---

**文档生成时间**：2026-06-19

**文档版本**：v1.0
